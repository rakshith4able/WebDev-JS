//We can change style of the HTML elements using Javascript

//Changing H1 color to Blue

const h1 = document.querySelector('h1');

h1.style.color = 'red';
h1.style.fontSize = '5em';// This way adds the style as inline style in HTML. SO its not preferred. 

//Other approach is creating styles in classes